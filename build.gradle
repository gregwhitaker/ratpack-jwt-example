plugins {
    id 'com.gradle.build-scan' version '1.9.1'
}

buildScan {
    licenseAgreementUrl = 'https://gradle.com/terms-of-service'
    licenseAgree = 'yes'
    publishAlways()
}

allprojects {
    repositories {
        jcenter()
    }
}
